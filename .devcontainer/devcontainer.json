{
  "name": "dotfiles",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/go:1": {
      "version": "latest"
    }
  },
  "postCreateCommand": "sudo apt-get install -y shellcheck && sh -c \"$(curl -fsLS get.chezmoi.io)\" -- -b ~/.local/bin && ~/.local/bin/chezmoi init --apply --source \"$PWD\" --promptString machineRole=ephemeral",
  "customizations": {
    "vscode": {
      "extensions": [
        "timonwong.shellcheck",
        "mads-hartmann.bash-ide-vscode"
      ]
    }
  }
}
