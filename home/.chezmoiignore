{{ if .isEphemeral }}
.tmux.conf
.tmux/**
run_once_10-install-tpm.sh
run_once_40-install-rtk.sh
run_once_46-install-cloud-clis.sh
.config/claude-jira.json
{{ end }}
{{ if env "CI" }}
run_once_46-install-cloud-clis.sh
run_once_47-install-go.sh
run_once_49-install-dev-tools.sh
{{ end }}
{{/* `get`, not a bare .installDevTooling: a machine initialised before this
     prompt existed has no such key in its persisted config, and a direct
     reference is a hard template error there rather than a missing opt-in.
     Absent reads as false, which is also the default this prompt ships. */}}
{{ if not (get . "installDevTooling") }}
run_once_49-install-dev-tools.sh
{{ end }}
{{ if ne .chezmoi.os "windows" }}
AppData/**
{{ end }}
